<h1>Add a task</h1>

<%= form_for [:task_list, @task] do |f| %>
    <%= f.label :description %>
    <%= f.text_field :description %>

    <%= f.label :due_date %>
    <%= f.date_select :due_date %>
    
    <%= f.submit %>
<% end %>